# 系统测试记录

测试日期：2026-06-30

测试环境：

- 操作系统：Windows
- 运行目录：`D:\Course Project\movie-rating-system`
- 运行方式：Node.js 本地 HTTP 服务
- 访问地址：`http://127.0.0.1:3000`

## 一、静态与语法检查

已执行：

```powershell
node --check server.js
node --check public\app.js
node --check scripts\start-local.js
node --check scripts\stop-local.js
node --check scripts\api-smoke-test.js
node scripts\check-system.js
```

结果：

```text
系统文件和基础数据检查通过。
```

## 二、服务启动检查

已执行：

```powershell
node scripts\start-local.js
```

结果：

```text
服务已经在 http://127.0.0.1:3000 运行。
```

首页访问结果：

```text
HTTP 200 OK
```

## 三、API 冒烟测试

已执行：

```powershell
node scripts\api-smoke-test.js
```

测试覆盖：

1. 管理员登录。
2. 首页仪表盘统计接口。
3. 电影组合查询接口。
4. 电影评分提交接口。
5. 按演员查询参演电影接口。

结果：

```text
API 冒烟测试通过：登录、仪表盘、电影查询、评分更新、演员电影查询均正常。
```

## 四、Word 报告说明

已生成：

```text
reports\电影评分系统课程设计报告.docx
docs\课程设计报告素材.md
```

结构检查结果：

```text
一、需求分析: True
二、概念结构设计: True
三、逻辑结构设计: True
四、数据库的物理设计: True
五、数据库设计实现及运行: True
六、系统详细设计及实现: True
七、总结: True
```

说明：用户已明确表示不用继续做 Word 页面渲染检查，因此未进行后续页面图片渲染检查。

